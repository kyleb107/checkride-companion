# Walkthrough screens

Three extra screens used only by the team's internal demo walkthrough, which
needs a public URL for every screen it narrates.

They are deliberately **not** in the project README's screenshot grid: the
logbook is an empty state, the lesson preview only makes sense mid-flow, and
the placement screen is a question rather than a result. The grid above is the
curated six.

Safe to delete once the walkthrough no longer needs them.
